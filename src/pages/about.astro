---
// src/pages/about.astro
import { SITE_CONFIG, ABOUT_CONFIG } from '../config';
import Navbar from '../components/Navbar.astro';
import Earth from '../components/Earth.astro';

// 自動解析中英文標籤線
const [yearEn, yearZh] = ABOUT_CONFIG.retirement.labels.year.split(' / ');
const [monthEn, monthZh] = ABOUT_CONFIG.retirement.labels.month.split(' / ');
const [dayEn, dayZh] = ABOUT_CONFIG.retirement.labels.day.split(' / ');

// 動態拆分左側 Bento 標題的中英文
const [leftTitleEn, leftTitleZh] = ABOUT_CONFIG.retirement.leftTitle.split(' / ');
---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>About | {SITE_CONFIG.title}</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@300;400;700;900&family=Share+Tech+Mono&display=swap" rel="stylesheet">
  
  <style is:inline set:html={`
    :root {
      --page-theme-color: ${SITE_CONFIG.themeColor};
    }
  `} />
</head>
<body>

  <Navbar />
  
  <div class="earth-horizon-layer">
    <Earth />
  </div>

  <main class="about-cinematic-viewport opacity-0">
    
    <section class="story-stage active" id="stage-capabilities">
      <div class="stage-inner flex-center">
        <div class="avatar-wrapper anim-s1-avatar">
          <img src={ABOUT_CONFIG.avatar.src} alt={ABOUT_CONFIG.avatar.alt} class="about-avatar-img" />
        </div>
        
        <h1 class="stage-title-large anim-s1-title">About Me.</h1>
        
        <div class="skills-grid-wrapper">
          {ABOUT_CONFIG.skills.map((skill) => (
            <div class="unified-glass-card anim-s1-card">
              <div class="card-inner-brand">{skill.title}</div>
              <div class="card-inner-sub">{skill.sub}</div>
            </div>
          ))}
        </div>

        <p class="manifesto-claim anim-s1-claim">{ABOUT_CONFIG.manifesto}</p>
      </div>
    </section>

    <section class="story-stage" id="stage-mosaic">
      <div class="stage-inner flex-center">
        <div class="mosaic-content-holder">
          <h2 class="mosaic-main-header">{ABOUT_CONFIG.mosaicHeader.title}</h2>
          <p class="mosaic-main-sub">{ABOUT_CONFIG.mosaicHeader.subtitle}</p>
        </div>

        <div class="mosaic-cloud-flexbox">
          {ABOUT_CONFIG.mosaicMatrix.map((item) => (
            <div class="mosaic-drag-card" data-rot={item.rotate.replace('deg', '')}>
              <div class="card-tag">{item.tag}</div>
              <h4 class="card-title">{item.title}</h4>
              <div class="card-glare"></div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <section class="story-stage" id="stage-deepdive">
      <div class="stage-inner flex-center">
        <div class="spotlight-container">
          <div class="spotlight" id="spotlight-1"></div>
          <div class="spotlight" id="spotlight-2"></div>
          <div class="spotlight" id="spotlight-3"></div>
        </div>

        <div class="deepdive-content-holder">
          <h2 class="deepdive-main-header">{ABOUT_CONFIG.deepDives.sectionHeader}</h2>
          <p class="deepdive-main-sub">{ABOUT_CONFIG.deepDives.sectionSub}</p>
          
          <div class="deepdive-pillars-matrix">
            {ABOUT_CONFIG.deepDives.pillars.map((pillar) => (
              <div class="pillar-glass-block">
                <div class="pillar-inner-glow"></div>
                <h3 class="pillar-title">{pillar.title}</h3>
                <p class="pillar-desc">{pillar.desc}</p>
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>

    <section 
      class="story-stage" 
      id="stage-retirement" 
      data-start-date={ABOUT_CONFIG.retirement.startDate}
      data-target-date={ABOUT_CONFIG.retirement.targetDate}
    >
      <div class="stage-inner flex-center">
        
        <div class="retirement-content-holder">
          <h2 class="retirement-main-header">{ABOUT_CONFIG.retirement.title}</h2>
          <p class="retirement-main-sub">{ABOUT_CONFIG.retirement.subtitle}</p>
        </div>

        <div class="bento-container">
          
          <div class="bento-card bento-left anim-s4-left">
            <div class="bento-left-brand">
              <span class="brand-en">{leftTitleEn}</span>
              {leftTitleZh && <span class="brand-zh">{leftTitleZh}</span>}
            </div>
            <p class="bento-left-desc">{ABOUT_CONFIG.retirement.description}</p>
          </div>

          <div class="bento-middle-column">
            <div class="bento-card bento-mid-row anim-s4-mid">
              <div class="bento-mid-value-group">
                <svg class="bento-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect><line x1="16" y1="2" x2="16" y2="6"></line><line x1="8" y1="2" x2="8" y2="6"></line><line x1="3" y1="10" x2="21" y2="10"></line></svg>
                <span class="countdown-value" id="val-year">00</span>
              </div>
              <div class="bento-mid-label-group">
                <span class="label-en">{yearEn}</span>
                <span class="label-zh">{yearZh}</span>
              </div>
            </div>
            
            <div class="bento-card bento-mid-row anim-s4-mid">
              <div class="bento-mid-value-group">
                <svg class="bento-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"></path></svg>
                <span class="countdown-value" id="val-month">00</span>
              </div>
              <div class="bento-mid-label-group">
                <span class="label-en">{monthEn}</span>
                <span class="label-zh">{monthZh}</span>
              </div>
            </div>
            
            <div class="bento-card bento-mid-row anim-s4-mid">
              <div class="bento-mid-value-group">
                <svg class="bento-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"></circle><polyline points="12 6 12 12 16 14"></polyline></svg>
                <span class="countdown-value" id="val-day">00</span>
              </div>
              <div class="bento-mid-label-group">
                <span class="label-en">{dayEn}</span>
                <span class="label-zh">{dayZh}</span>
              </div>
            </div>
          </div>

          <div class="bento-card bento-right anim-s4-right">
            
            <div class="station-clock-wrapper">
              <div class="flap-group">
                <div class="flap-card" id="card-h1">
                  <div class="card-half top"><div class="digit-inner">0</div></div>
                  <div class="card-half bottom"><div class="digit-inner">0</div></div>
                  <div class="flip-line"></div>
                </div>
                <div class="flap-card" id="card-h2">
                  <div class="card-half top"><div class="digit-inner">0</div></div>
                  <div class="card-half bottom"><div class="digit-inner">0</div></div>
                  <div class="flip-line"></div>
                </div>
              </div>
              <span class="clock-colon">:</span>
              <div class="flap-group">
                <div class="flap-card" id="card-m1">
                  <div class="card-half top"><div class="digit-inner">0</div></div>
                  <div class="card-half bottom"><div class="digit-inner">0</div></div>
                  <div class="flip-line"></div>
                </div>
                <div class="flap-card" id="card-m2">
                  <div class="card-half top"><div class="digit-inner">0</div></div>
                  <div class="card-half bottom"><div class="digit-inner">0</div></div>
                  <div class="flip-line"></div>
                </div>
              </div>
              <span class="clock-colon">:</span>
              <div class="flap-group">
                <div class="flap-card" id="card-s1">
                  <div class="card-half top"><div class="digit-inner">0</div></div>
                  <div class="card-half bottom"><div class="digit-inner">0</div></div>
                  <div class="flip-line"></div>
                </div>
                <div class="flap-card" id="card-s2">
                  <div class="card-half top"><div class="digit-inner">0</div></div>
                  <div class="card-half bottom"><div class="digit-inner">0</div></div>
                  <div class="flip-line"></div>
                </div>
              </div>
            </div>
            
            <div class="clock-caption">{ABOUT_CONFIG.retirement.clockCaption}</div>

            <div class="bento-divider"></div>
            
            <div class="bento-progress-anchor">
              <div class="progress-status-row">
                <span class="progress-mini-label">Total Era Progression</span>
                <span class="progress-percentage" id="progress-text">00.000000%</span>
              </div>
              <div class="progress-bar-track">
                <div class="progress-bar-fill" id="progress-bar-fill"></div>
              </div>
            </div>

          </div>

        </div>
      </div>
    </section>

    <div class="floating-dock-island">
      <div class="dock-blur-bg"></div>
      <div class="dock-buttons-row">
        {ABOUT_CONFIG.dockLabels.map((item, index) => (
          <button class={index === 0 ? "dock-btn active" : "dock-btn"} data-target={item.target}>
            <span class="dot"></span><span class="label">{item.label}</span>
          </button>
        ))}
      </div>
    </div>

  </main>

</body>
</html>

<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js" is:inline></script>
<script is:inline define:vars={{ defaultStart: ABOUT_CONFIG.retirement.startDate, defaultTarget: ABOUT_CONFIG.retirement.targetDate }}>
  document.addEventListener('DOMContentLoaded', () => {
    const viewport = document.querySelector('.about-cinematic-viewport');
    if (viewport) viewport.classList.remove('opacity-0');

    let currentStage = 0;
    const stages = [
      document.getElementById('stage-capabilities'),
      document.getElementById('stage-mosaic'),
      document.getElementById('stage-deepdive'),
      document.getElementById('stage-retirement')
    ];
    const dockButtons = document.querySelectorAll('.dock-btn');

    // ========================================================
    // 時鐘引擎與高精度微秒級流逝渲染軸
    // ========================================================
    const retirementSection = document.getElementById('stage-retirement');
    const targetDateStr = retirementSection?.dataset.targetDate || defaultTarget;
    const startDateStr = retirementSection?.dataset.startDate || defaultStart;
    
    const targetDate = new Date(targetDateStr);
    const startDate = new Date(startDateStr); 
    const totalTimeSpan = targetDate - startDate;

    function setFlapDigitWithAnimation(cardId, newValue) {
      const card = document.getElementById(cardId);
      if (!card) return;
      const topInner = card.querySelector('.top .digit-inner');
      const bottomInner = card.querySelector('.bottom .digit-inner');

      if (topInner.textContent === newValue) return;

      gsap.killTweensOf(card);
      topInner.textContent = newValue;
      bottomInner.textContent = newValue;
      
      gsap.fromTo(card, 
        { rotationX: 45, transformOrigin: "center top" }, 
        { rotationX: 0, duration: 0.28, ease: "back.out(3.5)", clearProps: "transform" }
      );
    }

    function updateCountdown() {
      const now = new Date();
      const diffTime = targetDate - now;
      if (diffTime <= 0) return;

      let years = targetDate.getFullYear() - now.getFullYear();
      let months = targetDate.getMonth() - now.getMonth();
      let days = targetDate.getDate() - now.getDate();

      if (days < 0) {
        months -= 1;
        const previousMonth = new Date(now.getFullYear(), now.getMonth(), 0);
        days += previousMonth.getDate();
      }
      if (months < 0) {
        years -= 1;
        months += 12;
      }

      const rHours = String(23 - now.getHours()).padStart(2, '0');
      const rMinutes = String(59 - now.getMinutes()).padStart(2, '0');
      const rSeconds = String(59 - now.getSeconds()).padStart(2, '0');

      document.getElementById('val-year').textContent = String(years).padStart(2, '0');
      document.getElementById('val-month').textContent = String(months).padStart(2, '0');
      document.getElementById('val-day').textContent = String(days).padStart(2, '0');

      setFlapDigitWithAnimation('card-h1', rHours[0]);
      setFlapDigitWithAnimation('card-h2', rHours[1]);
      setFlapDigitWithAnimation('card-m1', rMinutes[0]);
      setFlapDigitWithAnimation('card-m2', rMinutes[1]);
      setFlapDigitWithAnimation('card-s1', rSeconds[0]);
      setFlapDigitWithAnimation('card-s2', rSeconds[1]);
    }

    function renderHighPrecisionProgress() {
      const now = new Date();
      const elapsedTime = now - startDate;
      let currentPercent = (elapsedTime / totalTimeSpan) * 100;
      
      if (currentPercent > 100) currentPercent = 100;
      if (currentPercent < 0) currentPercent = 0;

      const progressText = document.getElementById('progress-text');
      const progressBarFill = document.getElementById('progress-bar-fill');

      if (progressText) progressText.textContent = currentPercent.toFixed(6) + '%';
      if (progressBarFill) progressBarFill.style.width = currentPercent + '%';

      requestAnimationFrame(renderHighPrecisionProgress);
    }

    setInterval(updateCountdown, 1000);
    updateCountdown();
    requestAnimationFrame(renderHighPrecisionProgress);

    // ========================================================
    // 舞台流轉切換總線
    // ========================================================
    function animateStage0In() {
      gsap.set([".anim-s1-avatar", ".anim-s1-title", ".anim-s1-card", ".anim-s1-claim"], { clearProps: "all" });
      gsap.timeline()
        .fromTo(".anim-s1-avatar", { opacity: 0, scale: 0.4, y: 35 }, { opacity: 1, scale: 1, y: 0, duration: 1.1, ease: "back.out(1.4)" })
        .fromTo(".anim-s1-title", { opacity: 0, y: 40 }, { opacity: 1, y: 0, duration: 0.9, ease: "expo.out" }, "-=0.8")
        .fromTo(".anim-s1-card", { opacity: 0, y: 45, scale: 0.96 }, { opacity: 1, y: 0, scale: 1, stagger: 0.06, duration: 1.0, ease: "back.out(1.3)" }, "-=0.6")
        .fromTo(".anim-s1-claim", { opacity: 0, y: 20 }, { opacity: 1, y: 0, duration: 0.8, ease: "power3.out" }, "-=0.45");
    }

    function initDragEngine() {
      document.querySelectorAll('.mosaic-drag-card').forEach(card => {
        let isDragging = false;
        card.addEventListener('mousedown', (e) => {
          e.preventDefault(); isDragging = true; card.style.cursor = 'grabbing'; card.style.zIndex = '100'; 
          let transformX = gsap.getProperty(card, "x") || 0;
          let transformY = gsap.getProperty(card, "y") || 0;
          const startX = e.clientX - transformX; const startY = e.clientY - transformY;
          const onMouseMove = (ev) => {
            if (!isDragging) return;
            transformX = ev.clientX - startX; transformY = ev.clientY - startY;
            gsap.set(card, { x: transformX, y: transformY, scale: 1.04, rotation: parseFloat(card.getAttribute('data-rot')) || 0 });
          };
          const onMouseUp = () => {
            isDragging = false; card.style.cursor = 'grab'; card.style.zIndex = '5'; gsap.set(card, { scale: 1 });
            document.removeEventListener('mousemove', onMouseMove); document.removeEventListener('mouseup', onMouseUp);
          };
          document.addEventListener('mousemove', onMouseMove); document.addEventListener('mouseup', onMouseUp);
        });
      });
    }

    initDragEngine();

    function switchStage(targetIndex) {
      if (targetIndex === currentStage) return;
      const outboundSection = stages[currentStage];
      const inboundSection = stages[targetIndex];

      dockButtons.forEach((btn, idx) => btn.classList.toggle('active', idx === targetIndex));
      inboundSection.classList.add('active');
      
      const transitionTimeline = gsap.timeline({
        onComplete: () => {
          outboundSection.classList.remove('active');
          gsap.set(outboundSection, { clearProps: "all" }); 
        }
      });

      if (currentStage === 0) {
        transitionTimeline.to([".anim-s1-avatar", ".anim-s1-title", ".anim-s1-card", ".anim-s1-claim"], { opacity: 0, y: -35, scale: 0.96, stagger: 0.03, duration: 0.35, ease: "power4.in" });
      } else if (currentStage === 1) {
        transitionTimeline.to(".mosaic-content-holder", { opacity: 0, y: -15, duration: 0.25 }).to(".mosaic-drag-card", { opacity: 0, scale: 0.7, y: -30, stagger: 0.02, duration: 0.3, ease: "power4.in" }, "-=0.2");
      } else if (currentStage === 2) {
        transitionTimeline.to(".pillar-glass-block", { opacity: 0, y: 30, stagger: 0.03, duration: 0.3, ease: "power3.in" }).to(".deepdive-content-holder, .spotlight-container", { opacity: 0, duration: 0.25 }, "-=0.15");
      } else if (currentStage === 3) {
        transitionTimeline.to(".retirement-content-holder", { opacity: 0, y: -20, duration: 0.25 })
                          .to([".anim-s4-left", ".anim-s4-mid", ".anim-s4-right"], { opacity: 0, y: -30, scale: 0.96, stagger: 0.03, duration: 0.3, ease: "power3.in" }, "-=0.15");
      }

      if (targetIndex === 0) {
        transitionTimeline.add(() => { animateStage0In(); }, "-=0.15");
      } 
      else if (targetIndex === 1) {
        transitionTimeline.add(() => {
          gsap.set([".mosaic-content-holder", ".mosaic-drag-card"], { clearProps: "all" });
          gsap.timeline()
            .fromTo(".mosaic-content-holder", { opacity: 0, y: 20 }, { opacity: 1, y: 0, duration: 0.6, ease: "expo.out" })
            .fromTo(".mosaic-drag-card", { opacity: 0, scale: 0.75, y: 40, rotation: 0 }, { opacity: 1, scale: 1, y: 0, rotation: (i, t) => parseFloat(t.getAttribute('data-rot')) || 0, stagger: 0.04, duration: 0.9, ease: "back.out(1.3)" }, "-=0.4");
        }, "-=0.15");
      } 
      else if (targetIndex === 2) {
        transitionTimeline.add(() => {
          gsap.set([".spotlight-container", ".spotlight", ".deepdive-content-holder", ".pillar-glass-block"], { clearProps: "all" });
          gsap.timeline()
            .fromTo(".spotlight-container", { opacity: 0 }, { opacity: 1, duration: 0.3 })
            .fromTo("#spotlight-1", { x: 0, scale: 0.7 }, { x: -window.innerWidth * 0.28, scale: 1, duration: 0.85, ease: "power4.out" })
            .fromTo("#spotlight-3", { x: 0, scale: 0.7 }, { x: window.innerWidth * 0.28, scale: 1, duration: 0.85, ease: "power4.out" }, "-=0.85")
            .fromTo(".spotlight", { opacity: 0 }, { opacity: 0.45, stagger: 0.04, duration: 0.7, ease: "power2.out" }, "-=0.65")
            .fromTo(".deepdive-content-holder", { opacity: 0, y: 15 }, { opacity: 1, y: 0, duration: 0.4, ease: "expo.out" }, "-=0.4")
            .fromTo(".pillar-glass-block", { opacity: 0, y: 35, scale: 0.97 }, { opacity: 1, y: 0, scale: 1, stagger: 0.06, duration: 0.8, ease: "back.out(1.3)" }, "-=0.25");
        }, "-=0.18");
      }
      else if (targetIndex === 3) {
        transitionTimeline.add(() => {
          gsap.set([".retirement-content-holder", ".anim-s4-left", ".anim-s4-mid", ".anim-s4-right"], { clearProps: "all" });
          gsap.timeline()
            .fromTo(".retirement-content-holder", { opacity: 0, y: 20 }, { opacity: 1, y: 0, duration: 0.6, ease: "expo.out" })
            .fromTo(".anim-s4-left", { opacity: 0, x: -35, scale: 0.97 }, { opacity: 1, x: 0, scale: 1, duration: 0.7, ease: "expo.out" }, "-=0.45")
            .fromTo(".anim-s4-mid", { opacity: 0, y: 25, scale: 0.96 }, { opacity: 1, y: 0, scale: 1, stagger: 0.05, duration: 0.7, ease: "back.out(1.2)" }, "-=0.5")
            .fromTo(".anim-s4-right", { opacity: 0, x: 35, scale: 0.96 }, { opacity: 1, x: 0, scale: 1, duration: 0.75, ease: "back.out(1.1)" }, "-=0.55");
        }, "-=0.15");
      }

      currentStage = targetIndex;
    }

    dockButtons.forEach(btn => {
      btn.addEventListener('click', (e) => {
        switchStage(parseInt(e.currentTarget.dataset.target, 10));
      });
    });
  });
</script>

<style>
  /* ========================================================
     全局布局體系
     ======================================================== */
  :global(html), :global(body) {
    margin: 0 !important;
    padding: 0 !important;
    background-color: #030303;
    color: #ffffff;
    width: 100% !important;
    height: 100vh !important;
    overflow: hidden !important;
    font-family: 'Noto Sans SC', sans-serif !important;
    -webkit-font-smoothing: antialiased;
  }

  .earth-horizon-layer {
    position: fixed; top: 72.5vh; left: 50%; transform: translateX(-50%) scale(2.0);
    width: 100vw; height: 100vh; pointer-events: none; z-index: 1; opacity: 0.85;
  }

  .about-cinematic-viewport { position: relative; z-index: 2; width: 100vw; height: 100vh; overflow: hidden; }
  .opacity-0 { opacity: 0 !important; }
  .story-stage { position: absolute; inset: 0; width: 100vw; height: 100vh; opacity: 0; pointer-events: none; visibility: hidden; }
  .story-stage.active { opacity: 1; pointer-events: auto; visibility: visible; }
  .stage-inner { width: 100%; height: 100%; box-sizing: border-box; }
  .flex-center { display: flex; flex-direction: column; justify-content: center; align-items: center; }

  /* 🔴 第一階段：核心能力卡精準引入 Dock 島暗色系（基底加厚，對齊質感） */
  .avatar-wrapper { margin-bottom: 24px; width: 88px; height: 88px; border-radius: 26px; padding: 1px; background: linear-gradient(180deg, rgba(255,255,255,0.25), transparent); box-shadow: 0 20px 40px rgba(0,0,0,0.6); }
  .about-avatar-img { width: 100%; height: 100%; border-radius: 25px; object-fit: cover; }
  .stage-title-large { font-size: 5.5rem; font-weight: 900; letter-spacing: -0.05em; margin: 0 0 45px 0; background: linear-gradient(180deg, #ffffff 50%, #888888 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
  .skills-grid-wrapper { display: flex; justify-content: center; gap: 16px; width: 90%; max-width: 1200px; }
  .unified-glass-card { flex: 1; padding: 35px 20px; border-radius: 24px; background: rgba(20, 20, 20, 0.6); border: 1px solid rgba(255, 255, 255, 0.08); box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.02), 0 20px 50px rgba(0, 0, 0, 0.3); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); text-align: center; transition: transform 0.6s cubic-bezier(0.34, 1.56, 0.64, 1), background 0.4s ease, border-color 0.4s ease; }
  .unified-glass-card:hover { transform: translateY(-8px) scale(1.03); background: rgba(30, 30, 30, 0.7); border-color: rgba(255, 255, 255, 0.2); }
  .card-inner-brand { font-weight: 900; font-size: 1.15rem; color: #ffffff; margin-bottom: 8px; letter-spacing: -0.02em; }
  .card-inner-sub { font-size: 0.75rem; color: #888888; letter-spacing: 0.15em; font-weight: 700; }
  .manifesto-claim { margin-top: 55px; font-size: 1.15rem; font-weight: 400; color: rgba(255, 255, 255, 0.75); }

  /* 🔴 第二階段：矩陣雲同步看齊 Dock 島基底 */
  .mosaic-content-holder { text-align: center; margin-bottom: 40px; }
  .mosaic-main-header { font-size: 2.8rem; font-weight: 900; letter-spacing: -0.03em; margin: 0 0 8px 0; background: linear-gradient(180deg, #ffffff 50%, #888888 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
  .mosaic-main-sub { font-size: 1rem; color: #888888; margin: 0; }
  .mosaic-cloud-flexbox { display: flex; flex-wrap: wrap; justify-content: center; align-items: center; gap: 28px; max-width: 1350px; padding: 0 40px; width: 90%; }
  .mosaic-drag-card { position: relative; padding: 24px; border-radius: 20px; background: rgba(20, 20, 20, 0.6); border: 1px solid rgba(255, 255, 255, 0.08); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.04), 0 20px 40px rgba(0, 0, 0, 0.4); width: fit-content; max-width: 28rem; cursor: grab; user-select: none; z-index: 5; transition: background 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease; }
  .mosaic-drag-card:hover { background: rgba(30, 30, 30, 0.7); border-color: rgba(255, 255, 255, 0.2); box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.06), 0 25px 50px rgba(0, 0, 0, 0.5); }
  .card-tag { font-size: 0.65rem; font-weight: 900; text-transform: uppercase; color: var(--page-theme-color); letter-spacing: 0.1em; margin-bottom: 6px; }
  .card-title { font-size: 0.9rem; font-weight: 700; line-height: 1.4; color: #ffffff; margin: 0; }

  /* 第三階段：高級劇場級錐形聚光燈 */
  .spotlight-container { 
    position: absolute; inset: 0; width: 100vw; height: 100vh; display: flex; justify-content: center; pointer-events: none; z-index: 1; 
  }
  .spotlight { 
    position: absolute; 
    top: -10vh; 
    width: 75vw; 
    height: 115vh; 
    background: conic-gradient(at top center,
      transparent 0%,
      transparent 43%,
      color-mix(in srgb, var(--page-theme-color) 4%, transparent) 46.5%,
      color-mix(in srgb, var(--page-theme-color) 12%, #ffffff) 50%, 
      color-mix(in srgb, var(--page-theme-color) 4%, transparent) 53.5%,
      transparent 57%,
      transparent 100%
    );
    filter: blur(32px);
    mask-image: linear-gradient(180deg, rgba(0,0,0,0.9) 0%, transparent 92%);
    -webkit-mask-image: linear-gradient(180deg, rgba(0,0,0,0.9) 0%, transparent 92%);
    mix-blend-mode: screen; 
    opacity: 0; 
    transform-origin: top center;
  }

  .deepdive-content-holder { position: relative; z-index: 2; width: 90%; max-width: 1400px; text-align: center; }
  .deepdive-main-header { font-size: 2.8rem; font-weight: 900; letter-spacing: -0.03em; margin: 0 0 8px 0; background: linear-gradient(180deg, #ffffff 50%, #888888 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
  .deepdive-main-sub { font-size: 1rem; color: #888888; margin-bottom: 60px; }
  .deepdive-pillars-matrix { display: flex; gap: 24px; width: 100%; }
  /* 🔴 第三階段：深色模式三大支柱卡片同步升級 Dock 島基底 */
  .pillar-glass-block { flex: 1; position: relative; padding: 40px 32px; border-radius: 24px; background: rgba(20, 20, 20, 0.6); border: 1px solid rgba(255, 255, 255, 0.08); backdrop-filter: blur(24px); -webkit-backdrop-filter: blur(24px); box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.03), 0 20px 50px rgba(0, 0, 0, 0.3); text-align: left; overflow: hidden; transition: transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1); }
  .pillar-glass-block:hover { transform: translateY(-8px) scale(1.02); background: rgba(30, 30, 30, 0.7); border-color: rgba(255, 255, 255, 0.15); }
  .pillar-title { font-size: 1.25rem; font-weight: 700; color: #ffffff; margin: 0 0 16px 0; }
  .pillar-desc { font-size: 0.9rem; color: #aaaaaa; line-height: 1.6; margin: 0; }

  /* 第四階段置頂看板 */
  .retirement-content-holder { text-align: center; margin-bottom: 35px; width: 100%; }
  .retirement-main-header { font-size: 2.8rem; font-weight: 900; letter-spacing: -0.03em; margin: 0 0 8px 0; background: linear-gradient(180deg, #ffffff 50%, #888888 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
  .retirement-main-sub { font-size: 1rem; color: #888888; margin: 0; }

  /* 第四階段 Bento 核心網格 */
  .bento-container {
    display: grid;
    grid-template-columns: 1.1fr 1.2fr 1.7fr;
    gap: 24px;
    width: 90%;
    max-width: 1350px;
    height: 440px;
    box-sizing: border-box;
  }

  /* 🔴 第四階段：Bento 看板群落全面切換至 Dock 島的高級沉浸暗色調 */
  .bento-card {
    background: rgba(20, 20, 20, 0.6);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 28px;
    backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.02), 0 25px 50px rgba(0, 0, 0, 0.3);
    padding: 35px;
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    overflow: hidden;
  }

  .bento-left { justify-content: space-between; text-align: left; }
  
  /* 兩行字體大小、字重、顏色完全徹底一致 */
  .bento-left-brand { display: flex; flex-direction: column; gap: 6px; }
  .bento-left-brand span { font-size: 1.6rem; font-weight: 900; letter-spacing: -0.02em; color: #ffffff; }
  
  .bento-left-desc { font-size: 0.85rem; color: #777777; line-height: 1.6; margin: 0; }

  .bento-middle-column { display: flex; flex-direction: column; gap: 16px; height: 100%; }
  .bento-mid-row {
    flex: 1; padding: 0 24px; flex-direction: row; align-items: center; justify-content: space-between; gap: 12px;
  }
  .bento-mid-value-group { display: flex; align-items: center; gap: 14px; }
  .bento-icon { width: 22px; height: 22px; color: var(--page-theme-color); opacity: 0.85; flex-shrink: 0; }
  .countdown-value { font-family: 'Share Tech Mono', monospace; font-size: 2.5rem; font-weight: 700; color: #ffffff; }
  
  .bento-mid-label-group { display: flex; flex-direction: column; text-align: right; gap: 2px; }
  .bento-mid-label-group .label-en { font-size: 0.65rem; font-weight: 700; color: #444446; text-transform: uppercase; letter-spacing: 0.05em; }
  .bento-mid-label-group .label-zh { font-size: 0.8rem; font-weight: 700; color: #88888b; }

  .bento-right {
    justify-content: center; align-items: center; position: relative;
    padding: 30px 35px 25px 35px;
  }
  
  .station-clock-wrapper {
    display: flex; align-items: center; gap: 10px; background: #080809;
    padding: 24px 35px; border-radius: 24px;
    box-shadow: inset 0 4px 30px rgba(0,0,0,0.8), 0 4px 20px rgba(0,0,0,0.5);
    border: 1px solid rgba(255, 255, 255, 0.03);
  }

  .flap-group { display: flex; gap: 4px; }
  .flap-card {
    position: relative; width: 54px; height: 84px; background: #1c1c1e; border-radius: 10px;
    box-shadow: 0 8px 16px rgba(0,0,0,0.5); transform-style: preserve-3d; perspective: 300px;
  }
  
  .card-half { position: absolute; left: 0; width: 100%; height: 42px; overflow: hidden; }
  .card-half.top { top: 0; background: #1c1c1e; border-radius: 10px 10px 0 0; border-bottom: 1px solid rgba(0, 0, 0, 0.7); }
  .card-half.bottom { bottom: 0; background: #141416; border-radius: 0 0 10px 10px; }
  
  .digit-inner {
    position: absolute; left: 0; width: 100%; height: 84px; line-height: 84px;
    display: flex; justify-content: center; align-items: center;
    font-family: 'Share Tech Mono', monospace; font-size: 4.8rem; font-weight: 900; color: #ffffff;
  }
  .card-half.top .digit-inner { top: 0; }
  .card-half.bottom .digit-inner { top: -42px; }
  
  .flip-line { position: absolute; top: 50%; left: 0; width: 100%; height: 1px; background: rgba(0, 0, 0, 0.5); z-index: 10; box-shadow: 0 1px 0 rgba(255, 255, 255, 0.04); }
  .clock-colon { font-family: 'Share Tech Mono', monospace; font-size: 3.5rem; color: rgba(255,255,255,0.15); animation: pulse 1s infinite; padding: 0 2px; }
  .clock-caption { font-size: 0.65rem; font-weight: 900; color: rgba(255,255,255,0.12); letter-spacing: 0.2em; margin-top: 16px; margin-bottom: 0px; }

  @keyframes pulse { 50% { opacity: 0.3; } }

  .bento-divider {
    width: 100%; height: 1px; background: rgba(255, 255, 255, 0.08); margin: 18px 0 14px 0; 
  }

  /* 壓艙石與 Google Sans 進度條樣式 */
  .bento-progress-anchor {
    margin-top: 2rem; width: 100%; display: flex; flex-direction: column; gap: 8px; box-sizing: border-box;
  }
  .progress-status-row { width: 100%; display: flex; justify-content: space-between; align-items: center; }
  .progress-mini-label { font-size: 0.65rem; font-weight: 700; color: rgba(255, 255, 255, 0.25); text-transform: uppercase; letter-spacing: 0.08em; }
  
  .progress-percentage {
    font-family: 'Google Sans', 'Product Sans', -apple-system, BlinkMacSystemFont, sans-serif;
    font-size: 1.05rem; font-weight: 700; color: var(--page-theme-color); letter-spacing: -0.01em;
  }

  .progress-bar-track { width: 100%; height: 4px; background: rgba(255, 255, 255, 0.05); border-radius: 2px; overflow: hidden; position: relative; }
  .progress-bar-fill {
    height: 100%; width: 0%; background: var(--page-theme-color); box-shadow: 0 0 10px var(--page-theme-color); border-radius: 2px;
  }

  /* Dock 導航島 */
  .floating-dock-island {
    position: fixed; bottom: 40px; left: 50%; transform: translateX(-50%); z-index: 999;
    display: flex; align-items: center; padding: 8px; border-radius: 30px;
    border: 1px solid rgba(255, 255, 255, 0.08); background: rgba(20, 20, 20, 0.35);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.6), inset 0 0 0 1px rgba(255,255,255,0.05); overflow: hidden;
  }
  .dock-blur-bg { position: absolute; inset: 0; backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); z-index: -1; }
  .dock-buttons-row { display: flex; gap: 4px; position: relative; z-index: 2; }
  .dock-btn { background: transparent; border: none; outline: none; display: flex; align-items: center; gap: 8px; padding: 10px 22px; border-radius: 22px; cursor: pointer; color: rgba(255, 255, 255, 0.4); transition: all 0.35s cubic-bezier(0.16, 1, 0.3, 1); }
  .dock-btn .dot { display: inline-block !important; width: 6px !important; height: 6px !important; border-radius: 50% !important; background: rgba(255, 255, 255, 0.3); flex-shrink: 0 !important; transition: all 0.35s ease; }
  .dock-btn .label { font-size: 0.8rem; font-weight: 700; }
  .dock-btn:hover { color: rgba(255, 255, 255, 0.75); }
  .dock-btn.active { color: #ffffff; background: rgba(255, 255, 255, 0.09); box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2), inset 0 0 0 1px rgba(255, 255, 255, 0.05); }
  .dock-btn.active .dot { background: var(--page-theme-color) !important; box-shadow: 0 0 12px var(--page-theme-color) !important; }

  /* ========================================================
     Light Mode 明亮形態全面調優軸
     ======================================================== */
  :global(.light-mode) .stage-title-large, :global(.light-mode) .mosaic-main-header, :global(.light-mode) .deepdive-main-header, :global(.light-mode) .retirement-main-header { background: linear-gradient(180deg, #1d1d1f 50%, #86868b 100%); -webkit-background-clip: text; }
  :global(.light-mode) .manifesto-claim, :global(.light-mode) .mosaic-main-sub, :global(.light-mode) .deepdive-main-sub, :global(.light-mode) .retirement-main-sub { color: #515154; }
  
  /* 🔴 明亮模式核心修正：將不透明度一舉從偏透的0.28平滑拉升至0.55，補回實體色彩骨架，同步保留全量毛玻璃折射 */
  :global(.light-mode) .unified-glass-card, 
  :global(.light-mode) .mosaic-drag-card, 
  :global(.light-mode) .pillar-glass-block, 
  :global(.light-mode) .bento-card { 
    background: rgba(255, 255, 255, 0.55) !important; 
    backdrop-filter: blur(20px) !important;
    -webkit-backdrop-filter: blur(20px) !important;
    border: 1px solid rgba(0, 0, 0, 0.06) !important; 
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.4), 0 20px 40px rgba(0, 0, 0, 0.03); 
  }
  
  :global(.light-mode) .card-inner-brand, :global(.light-mode) .card-title, :global(.light-mode) .pillar-title { color: #1d1d1f; }
  
  /* 明亮模式下中英文雙行標題平權色彩同步 */
  :global(.light-mode) .bento-left-brand span { color: #1d1d1f; }
  
  :global(.light-mode) .pillar-desc, :global(.light-mode) .bento-left-desc { color: #515154; }
  :global(.light-mode) .countdown-value { color: #1d1d1f; text-shadow: none; }
  
  /* 淺色投射平權 */
  :global(.light-mode) .spotlight-container { mix-blend-mode: normal; }
  :global(.light-mode) .spotlight {
    background: conic-gradient(at top center,
      transparent 0%,
      transparent 43%,
      color-mix(in srgb, var(--page-theme-color) 4%, transparent) 46.5%,
      var(--page-theme-color) 50%, 
      color-mix(in srgb, var(--page-theme-color) 4%, transparent) 53.5%,
      transparent 57%,
      transparent 100%
    );
  }

  :global(.light-mode) .progress-percentage { color: var(--page-theme-color) !important; text-shadow: none; }
  :global(.light-mode) .dock-btn.active .dot { background: var(--page-theme-color) !important; box-shadow: 0 0 10px var(--page-theme-color) !important; }

  :global(.light-mode) .bento-mid-label-group .label-en { color: #86868b; }
  :global(.light-mode) .bento-mid-label-group .label-zh { color: #424245; }
  :global(.light-mode) .station-clock-wrapper { background: #eaeaea; border-color: rgba(0,0,0,0.06); box-shadow: inset 0 2px 10px rgba(0,0,0,0.06); }
  :global(.light-mode) .flap-card { background: #ffffff; box-shadow: 0 4px 10px rgba(0,0,0,0.06); }
  :global(.light-mode) .card-half.top { background: #ffffff; border-bottom-color: #e5e5ea; }
  :global(.light-mode) .card-half.bottom { background: #f2f2f7; }
  :global(.light-mode) .digit-inner { color: #1d1d1f; }
  :global(.light-mode) .clock-colon { color: rgba(0,0,0,0.2); }
  :global(.light-mode) .clock-caption { color: #86868b; }
  :global(.light-mode) .bento-divider { background: rgba(0, 0, 0, 0.06); }
  :global(.light-mode) .progress-bar-track { background: rgba(0, 0, 0, 0.05); }
  :global(.light-mode) .progress-mini-label { color: rgba(0, 0, 0, 0.35); }

  :global(.light-mode) .floating-dock-island { background: rgba(255, 255, 255, 0.45); border: 1px solid rgba(0, 0, 0, 0.06); box-shadow: 0 20px 40px rgba(0, 0, 0, 0.06); }
  :global(.light-mode) .dock-btn { color: rgba(0, 0, 0, 0.4); }
  :global(.light-mode) .dock-btn:hover { color: rgba(0, 0, 0, 0.6); }
  :global(.light-mode) .dock-btn.active { color: #1d1d1f; background: rgba(0, 0, 0, 0.06); box-shadow: inset 0 0 0 1px rgba(0, 0, 0, 0.02); }
  :global(.light-mode) .dock-btn .dot { background: rgba(0, 0, 0, 0.2); flex-shrink: 0 !important; }

  @media (max-width: 992px) {
    .bento-container { grid-template-columns: 1fr; height: auto; }
    .bento-divider { margin: 15px 0; }
  }
  @media (max-width: 768px) {
    .skills-grid-wrapper, .deepdive-pillars-matrix { flex-direction: column; }
    .stage-title-large { font-size: 3.5rem; }
    .floating-dock-island { width: 92%; padding: 4px; bottom: 20px; }
    .dock-btn { padding: 8px 12px; } .dock-btn .label { font-size: 0.7rem; }
    .earth-horizon-layer { top: 62vh; transform: translateX(-50%) scale(1.6); }
    .station-clock-wrapper { padding: 15px 20px; gap: 4px; }
    .flap-card { width: 34px; height: 54px; }
    .card-half { height: 27px; }
    .digit-inner { height: 54px; line-height: 54px; font-size: 2.8rem; }
    .card-half.bottom .digit-inner { top: -27px; }
  }
</style>